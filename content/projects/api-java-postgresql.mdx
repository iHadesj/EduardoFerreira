---
title: API Java + PostgreSQL
slug: api-java-postgresql
summary: Persistência relacional com JPA, camadas em MVC e CRUD testável sobre PostgreSQL.
problem: Modelar dados e expor um CRUD de forma limpa, separando responsabilidades e mantendo o banco sob controle.
date: 2024-03-01
cover: /projects/api-java-postgresql.png
coverAlt: Representação abstrata do fluxo de uma API Java em camadas conectada a um banco de dados PostgreSQL.
stack: [Java, Spring, JPA/Hibernate, PostgreSQL, JUnit, MVC]
repo: https://github.com/iHadesj/JPA_PostgreSQL
status: shipped
featured: true
order: 4
highlights:
  - Mapeamento JPA com entidades e relações explícitas
  - Camadas separadas no padrão MVC (controller → service → repository)
  - Consultas de exemplo versionadas em exemplos.sql
metrics:
  - label: Linguagem
    value: Java
  - label: Persistência
    value: JPA / PostgreSQL
  - label: Testes
    value: JUnit
decisions:
  - choice: JPA/Hibernate para a persistência
    instead: JDBC cru com SQL espalhado pela base
    because: O domínio fica mapeado em objetos e relações declarativas, legível sem caçar query.
    cost: Abre mão do controle fino sobre cada consulta — por isso as que importam ficam explícitas em exemplos.sql, à vista.
  - choice: PostgreSQL
    instead: Um banco embarcado só para o exercício
    because: Tipos e constraints empurram a integridade para o banco, não só para o código.
  - choice: Camadas em MVC — controller, service, repository
    instead: Regra de negócio dentro do controller
    because: Controller traduz HTTP, service guarda a regra e repository fala com o banco, o que permite testar a regra isolada do transporte e da persistência.
    cost: Mais arquivos e mais indireção do que um CRUD escrito em uma camada só.
en:
  title: Java API + PostgreSQL
  summary: Relational persistence with JPA, MVC layering and a testable CRUD over PostgreSQL.
  problem: Model the data and expose a CRUD cleanly, separating responsibilities and keeping the database under control.
  coverAlt: Abstract representation of a layered Java API connected to a PostgreSQL database.
  highlights:
    - JPA mapping with explicit entities and relations
    - MVC layering (controller → service → repository)
    - Reference queries versioned in exemplos.sql
  metrics:
    - label: Language
      value: Java
    - label: Persistence
      value: JPA / PostgreSQL
    - label: Tests
      value: JUnit
  decisions:
    - choice: JPA/Hibernate for persistence
      instead: Raw JDBC with SQL spread across the codebase
      because: The domain ends up mapped as objects and declarative relations, readable without hunting for a query.
      cost: Gives up fine control over each query — which is why the ones that matter stay explicit in exemplos.sql, out in the open.
    - choice: PostgreSQL
      instead: An embedded database just for the exercise
      because: Types and constraints push integrity into the database rather than leaving it all to the code.
    - choice: MVC layering — controller, service, repository
      instead: Business rules inside the controller
      because: The controller translates HTTP, the service holds the rule and the repository talks to the database, which makes the rule testable apart from transport and persistence.
      cost: More files and more indirection than a CRUD written in a single layer.
---

## O problema

Dois exercícios complementares — um focado em mapeamento JPA com PostgreSQL,
outro num CRUD organizado em MVC — tratam da mesma questão de fundo: como
modelar dados e expor operações de forma que o domínio fique legível, as
camadas não vazem responsabilidade umas nas outras, e o comportamento seja
testável sem subir a aplicação inteira.

## Decisões técnicas

- **JPA/Hibernate em vez de JDBC cru.** O ganho é mapear o domínio em objetos e
  relações declarativas em vez de espalhar SQL por toda a base. O trade-off
  consciente: abrir mão de controle fino sobre cada query — por isso as
  consultas que importam ficam explícitas em `exemplos.sql`, à vista.
- **PostgreSQL.** Banco relacional sério, gratuito, com tipos e constraints que
  empurram a integridade para o banco, não só para o código.
- **MVC (controller → service → repository).** Cada camada com um trabalho:
  controller traduz HTTP, service guarda a regra de negócio, repository fala com
  o banco. Facilita testar a regra isolada do transporte e da persistência.

## Arquitetura

Requisição entra pelo controller, que delega ao service (onde mora a regra) e
recorre ao repository para ler/gravar via JPA. As entidades carregam o mapeamento
objeto-relacional; o `exemplos.sql` documenta o schema e as consultas de
referência.

## Resultados

- Domínio modelado com entidades e relações explícitas.
- Regras isoladas em serviços, exercitadas por testes JUnit.
- **Métrica a coletar:** cobertura de testes e número de entidades/endpoints.

## O que eu faria diferente

Adicionaria migrations versionadas (Flyway/Liquibase) em vez de SQL solto,
DTOs para não vazar entidades JPA na borda HTTP, e testes de integração com um
PostgreSQL efêmero (Testcontainers) cobrindo o caminho completo.
