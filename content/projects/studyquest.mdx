---
title: StudyQuest
slug: studyquest
summary: Plataforma de estudos gamificada com trilhas por matéria, progressão persistida e partidas em tempo real.
problem: Manter constância nos estudos é difícil — o StudyQuest transforma a rotina em progressão visível e desafios curtos.
date: 2026-03-01
cover: /projects/studyquest.png
coverAlt: Tela inicial do StudyQuest com matérias organizadas por área e barras de progresso coloridas.
stack: [React, TypeScript, Express, Firebase, Socket.IO, Zustand]
repo: https://github.com/iHadesj/studyquest-monorepo
demo: https://go-studyquest.vercel.app
status: in-progress
featured: true
order: 2
highlights:
  - Conteúdo e exercícios organizados em 11 matérias com progressão por nível
  - XP, conquistas, ranking e perfil sincronizados com o Firebase
  - Duelos multiplayer com convites, salas e pontuação em tempo real
metrics:
  - label: Conteúdo
    value: 11 matérias
  - label: Tempo real
    value: Socket.IO
  - label: Persistência
    value: Firebase
decisions:
  - choice: Firebase para identidade e progresso
    instead: Autenticação e banco próprios no backend Express
    because: Authentication cuida da sessão e o Firestore mantém XP, estrelas, tentativas, níveis, perfil, amizades e conquistas, com um listener em tempo real hidratando a aplicação quando esses dados mudam.
    cost: Credenciais administrativas precisam ficar no gerenciador de secrets do deploy, nunca em arquivo versionável.
  - choice: Socket.IO para o multiplayer
    instead: Polling da API REST durante a partida
    because: Convites, salas, prontidão, respostas e placar trafegam como eventos, e a partida trabalha com tempo limitado e pontuação proporcional à velocidade da resposta.
    cost: O estado das salas vive na memória do processo, então hoje o servidor não escala para além de uma instância.
  - choice: Conteúdo em arquivos JSON por disciplina
    instead: Modelar as perguntas como tabelas no banco
    because: O GameManager reúne esse banco para sortear perguntas sem duplicar identificadores, e uma matéria nova é um arquivo, não uma migração.
  - choice: Zustand concentrando o progresso já sincronizado
    instead: Estado espalhado pelas telas de matéria, nível e exercício
    because: As telas consomem uma fonte só, e a hidratação vinda do Firebase tem um único ponto de entrada.
en:
  title: StudyQuest
  summary: A gamified study platform with subject tracks, persisted progression and real-time duels.
  problem: Studying consistently is hard — StudyQuest turns the routine into visible progression and short challenges.
  coverAlt: StudyQuest home screen with subjects grouped by area and colourful progress bars.
  highlights:
    - Content and exercises across 11 subjects with level-by-level progression
    - XP, achievements, ranking and profile synced with Firebase
    - Multiplayer duels with invites, rooms and real-time scoring
  metrics:
    - label: Content
      value: 11 subjects
    - label: Real time
      value: Socket.IO
    - label: Persistence
      value: Firebase
  decisions:
    - choice: Firebase for identity and progress
      instead: Rolling my own auth and database on the Express backend
      because: Authentication owns the session while Firestore holds XP, stars, attempts, levels, profile, friendships and achievements, with a realtime listener rehydrating the app when any of it changes.
      cost: Admin credentials have to live in the deploy environment's secret manager, never in a committable file.
    - choice: Socket.IO for multiplayer
      instead: Polling the REST API during a match
      because: Invites, rooms, readiness, answers and the scoreboard all travel as events, and matches run on a timer with scoring proportional to answer speed.
      cost: Room state lives in process memory, so the server does not scale past a single instance today.
    - choice: Content as per-subject JSON files
      instead: Modelling the question bank as database tables
      because: GameManager assembles that bank to draw questions without duplicating identifiers, and a new subject is a file rather than a migration.
    - choice: Zustand holding the already-synced progress
      instead: State scattered across the subject, level and exercise screens
      because: The screens read one source, and hydration coming back from Firebase has a single entry point.
---

## O problema

Estudar com constância é menos um problema de acesso ao conteúdo e mais um
problema de motivação: a recompensa costuma ser distante e o progresso,
invisível. O StudyQuest aproxima esse retorno ao transformar cada sessão em
uma sequência curta de conteúdo, exercícios e recompensas visíveis.

## Decisões técnicas

- **React, Vite e TypeScript no frontend.** A interface é dividida em telas de
  matéria, nível, conteúdo, exercícios, ranking, conquistas e multiplayer. O
  Zustand concentra o estado de progresso já sincronizado com o usuário.
- **Firebase para identidade e progresso.** O Firebase Authentication cuida da
  sessão, enquanto o Firestore mantém XP, estrelas, tentativas, níveis
  concluídos, perfil, amizades e conquistas. Um listener em tempo real hidrata
  a aplicação quando esses dados mudam.
- **Express e TypeScript no backend.** Rotas e controllers expõem matérias,
  exercícios e usuários. O conteúdo fica separado em arquivos JSON por
  disciplina, e o `GameManager` reúne esse banco para sortear perguntas sem
  duplicar identificadores.
- **Socket.IO no multiplayer.** Convites, salas, estado de prontidão, respostas
  e placar trafegam por eventos. As partidas trabalham com tempo limitado e
  pontuação proporcional à velocidade da resposta.

## Arquitetura

O repositório mantém duas aplicações no mesmo lugar: `frontend/` e `backend/`.
No cliente, as telas consomem a API REST por um serviço Axios e os eventos de
jogo pelo cliente Socket.IO; autenticação e atualizações do perfil vêm
diretamente do Firebase. No servidor, o Express encaminha as requisições para
controllers, enquanto o Socket.IO mantém o estado das salas e usa o Firebase
Admin para persistir as recompensas de XP.

## Resultados

- Jornada completa de autenticação, perfil, seleção de matéria, níveis,
  conteúdo e exercícios.
- Banco de perguntas cobrindo 11 matérias, de matemática e programação a
  história, inglês e música.
- Camada de gamificação com XP, estrelas, conquistas, ranking e modo
  Brainstorm.
- Experiência social com amizades, chat, convites e duelos multiplayer em tempo
  real.

## O que eu faria diferente

Formalizaria o repositório como um workspace para compartilhar tipos e schemas
de validação entre frontend e backend e centralizar os scripts de qualidade.
Também moveria o estado das salas para uma camada própria, com Redis para
permitir múltiplas instâncias, e ampliaria a cobertura automatizada dos fluxos
de progresso e multiplayer. Credenciais administrativas ficariam somente no
gerenciador de secrets do ambiente de deploy, nunca em arquivos versionáveis.
